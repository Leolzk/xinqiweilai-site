/** 活体蓝图 CRO 页：通过透明的项目协作流程展示研究目标、执行与交付之间的连续关系。 */
import { ClipboardCheck, FileOutput, Target } from "lucide-react";
import { ServiceHero } from "@/components/ServiceHero";
import { useLanguage } from "@/contexts/LanguageContext";
import { company, serviceCopy } from "@/lib/futureMindContent";

export default function Cro() { const { language } = useLanguage(); const zh = language === "zh"; const phases = [[Target, zh ? "研究目标对齐" : "Study objective alignment", zh ? "围绕研究问题、模型需求与预期路径建立共同起点。" : "Establish a shared starting point around research questions, model needs and expected pathways."],[ClipboardCheck, zh ? "项目执行协同" : "Project execution collaboration", zh ? "以透明的节点沟通支持实验推进与过程衔接。" : "Support experimental progress and workflow alignment through transparent milestone communication."],[FileOutput, zh ? "结果整理与交付" : "Result organization & delivery", zh ? "围绕项目输出梳理可追溯的结果信息与交付材料。" : "Organize traceable result information and delivery materials around project outputs."]]; return <div className="service-page"><ServiceHero {...serviceCopy.cro} code="04" /><section className="cro-intro"><div><p className="fm-eyebrow"><i />{zh ? "项目化研究协作" : "PROJECT-BASED RESEARCH COLLABORATION"}</p><h2>{zh ? <>让协作路径，<em>保持清晰与可追溯。</em></> : <>Keep collaboration<br /><em>clear and traceable.</em></>}</h2></div><p>{zh ? "类器官 CRO 服务并非黑箱式交付。我们强调在研究目标、实验过程与结果整理之间建立透明的协同节奏。" : "Organoid CRO services should not be a black box. We emphasize a transparent collaboration rhythm between study objectives, experimental progress and result organization."}</p></section><section className="cro-flow">{phases.map(([Icon, title, desc], index) => { const I = Icon as typeof Target; return <article key={title as string}><span>{String(index + 1).padStart(2,"0")}</span><div className="cro-flow__icon"><I size={24} /></div><h3>{title as string}</h3><p>{desc as string}</p></article>; })}</section><section className="cro-call"><span>{zh ? "需要讨论你的研究路径？" : "Need to discuss your research pathway?"}</span><a href={`tel:${company.telephone.replace(/-/g, "")}`}>{company.telephone}</a></section></div>; }
